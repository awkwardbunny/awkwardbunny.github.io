#!/bin/bash
git add .
curtime=`date`
git commit --allow-empty -m "Automatic Backup @ $curtime"
git push

#this script is run daily at midnight
#crontab -e
#then added: 0 0 * * * cd <working directory> && ./git.run
